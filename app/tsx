import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
// IMPORT TOASTER DI SINI
import { Toaster } from "sonner";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FlowSphere | Manajemen Proyek",
  description: "Aplikasi manajemen proyek dan tugas",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    // suppressHydrationWarning sangat penting untuk mencegah error Dark Mode dari Tailwind
    <html lang="id" suppressHydrationWarning>
      <body className={`${inter.className} antialiased selection:bg-brand-500/30`}>
        {/* TOASTER DIPASANG DI SINI AGAR MUNCUL DI SEMUA HALAMAN */}
        <Toaster richColors position="top-right" />
        {children}
      </body>
    </html>
  );
}